import {REGISTRATION_SUCCESS,
        REGiSTRATION_ERROR,
        GET_USER,
        LOGIN_SUCCESS,
        LOGIN_ERROR,
        SIGN_OFF
        } from "../../types";



export  const AuthReducer = (state, action) => {
    switch (action.type) {
        
    
        default:
            return state;
    }
}
