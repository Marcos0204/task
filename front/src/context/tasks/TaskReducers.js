// import { FORM_PROJECT,
//   GET_PROJECTS,
//   ADD_PROJECT,
//   VALIDATE_FORM,
//   ACTUAL_PROYECT,
//   DELETE_PROJECT } from '../../types/index'

export  const TaskReducers = (state, action) => {
  switch (action.type) {
      default:
          return state
  }
}